"use client"
import { FC, useEffect, useRef } from "react";

const Hero: FC = () => {

  return(
  <section>
    <div className="grid sticky top-0 md:grid-cols-12 md:h-screen items-center md:items-stretch">
    hero
    </div>
  </section>
)};

export default Hero;
